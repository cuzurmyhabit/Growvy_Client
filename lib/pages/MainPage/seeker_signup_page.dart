import 'package:flutter/material.dart';

class SeekerSignUpPage extends StatelessWidget {
  const SeekerSignUpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Job Seeker Home")),
      body: const Center(child: Text("Job Seeker Screen")),
    );
  }
}